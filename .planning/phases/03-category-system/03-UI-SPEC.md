---
phase: 03
slug: category-system
status: draft
shadcn_initialized: true
preset: base-nova (neutral)
created: 2026-04-09
---

# Phase 03 -- UI Design Contract

> Visual and interaction contract for the Category System. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> This phase adds category selection and system prompt editing to the existing battle flow. Users pick a category before starting a battle, each category applies a distinct system prompt, and users can optionally customize that prompt.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (initialized -- `components.json` exists) |
| Preset | base-nova, neutral base color, CSS variables |
| Component library | Radix UI (via shadcn/ui) |
| Icon library | lucide-react (shadcn default) |
| Font (Korean body) | Pretendard Variable (CDN, already in layout.tsx) |
| Font (UI / numbers) | Geist Sans (already in layout.tsx) |
| Font (code / mono) | Geist Mono (already in layout.tsx) |

### shadcn Components -- Phase 03 Additions

Already installed from Phase 02: `button`, `card`, `textarea`, `badge`, `separator`, `skeleton`

New components to install for Phase 03:

```bash
npx shadcn@latest add toggle-group collapsible
```

| Component | Purpose |
|-----------|---------|
| `toggle-group` | Category selector -- single-select group of category options |
| `collapsible` | System prompt editor -- expandable section for advanced users |

---

## Spacing Scale

Inherited from Phase 02. No changes.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-text gaps, inline badge padding |
| sm | 8px | Compact element spacing, category chip icon gap |
| md | 16px | Default element spacing, card padding inline |
| lg | 24px | Section padding, card padding block, category selector container padding |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Page top/bottom padding |
| 3xl | 64px | Not used in Phase 03 |

Exceptions: Category toggle items use minimum 44px touch target height (accessibility, same as Phase 02 vote buttons).

---

## Typography

Inherited from Phase 02. No changes to the scale.

| Role | Size | Weight | Line Height | Font Stack | Usage |
|------|------|--------|-------------|------------|-------|
| Body | 16px | 400 (regular) | 1.6 | Pretendard Variable | System prompt text, AI response text |
| Label | 14px | 400 (regular) | 1.4 | Pretendard Variable | Category labels, "system prompt" section label, metadata |
| Heading | 24px | 700 (bold) | 1.3 | Pretendard Variable | Page title (unchanged) |
| Mono | 14px | 400 (regular) | 1.5 | Geist Mono | Character count in prompt editor |

Notes:
- System prompt textarea uses 14px (Label size) to visually distinguish it from the main question input at 16px
- Category names use 14px label size. Selected toggle items use 700 (bold) weight; unselected toggle items use 400 (regular) weight. This provides a clear legibility distinction using only the two declared weights, complementing the `--primary` background fill that already communicates selection state.
- `word-break: keep-all` remains on all body text (inherited from Phase 02 globals.css)

---

## Color

Inherited from Phase 02 CSS variables. No new custom CSS variables required.

| Role | CSS Variable | Light Value | Usage in Phase 03 |
|------|-------------|-------------|-------------------|
| Dominant (60%) | `--background` | `oklch(1 0 0)` | Page background |
| Secondary (30%) | `--muted` | `oklch(0.97 0 0)` | Category selector container background, system prompt editor background |
| Border | `--border` | `oklch(0.922 0 0)` | Category toggle item borders, collapsible section border |
| Text primary | `--foreground` | `oklch(0.145 0 0)` | Category names, system prompt text |
| Text secondary | `--muted-foreground` | `oklch(0.556 0 0)` | Category descriptions, prompt placeholder, section labels |
| Accent (10%) | `--primary` | `oklch(0.546 0.245 262.881)` | Selected category toggle item background+border, CTA button |
| Accent text on primary | `--primary-foreground` | `oklch(1 0 0)` | Text on selected category toggle item |
| Destructive | `--destructive` | `oklch(0.577 0.245 27.325)` | Prompt character limit exceeded state only |

Accent (`--primary`) reserved for in Phase 03:
- Selected category toggle item (filled background)
- "battle start" primary CTA button (inherited from Phase 02)
- Active/focused border on system prompt textarea
- Streaming cursor (inherited from Phase 02)

No new custom color variables needed. Phase 03 uses only the existing shadcn palette.

---

## Component Inventory

### Phase 03 shadcn Components (to install)

| Component | Source | Purpose |
|-----------|--------|---------|
| ToggleGroup | shadcn `toggle-group` | Single-select category picker |
| Collapsible | shadcn `collapsible` | Expandable system prompt editor section |

### Existing shadcn Components (reused from Phase 02)

| Component | Purpose in Phase 03 |
|-----------|---------------------|
| Button | CTA, collapsible trigger, reset prompt button |
| Textarea | System prompt editor input |
| Badge | Category emoji label in selected state, prompt status indicator |

### Custom Components (to build)

| Component | Description |
|-----------|-------------|
| `CategorySelector` | ToggleGroup wrapper showing preset categories as selectable items. Each item: emoji icon + Korean label. Single selection enforced. Default selection: first category. |
| `SystemPromptEditor` | Collapsible section below the category selector. Shows the current system prompt for the selected category. Textarea for editing. Reset-to-default button. Character counter. |

---

## Preset Categories

These are the categories defined for v1. Each has an emoji icon, Korean label, and default system prompt.

| ID | Emoji | Korean Label | Default System Prompt |
|----|-------|-------------|----------------------|
| `general` | 💬 | 일반 | 당신은 한국어 AI 어시스턴트입니다. 사용자의 질문에 자연스러운 한국어로 답변하세요. 존댓말을 사용하고, 명확하고 유용한 답변을 제공하세요. |
| `homework` | 📚 | 과제 도움 | 당신은 한국 대학생을 돕는 학습 튜터입니다. 과제와 관련된 질문에 단계별로 설명해주세요. 정답만 알려주지 말고, 이해를 돕는 방식으로 가르쳐주세요. 존댓말을 사용하세요. |
| `cover-letter` | 📝 | 자기소개서 | 당신은 한국 취업 시장에 전문적인 자기소개서 코치입니다. 자기소개서 작성, 첨삭, 개선에 도움을 주세요. 한국 기업 문화와 채용 트렌드를 반영하세요. 존댓말을 사용하세요. |
| `counseling` | 🤗 | 고민 상담 | 당신은 따뜻하고 공감 능력이 뛰어난 상담사입니다. 사용자의 고민을 경청하고, 판단하지 않으며, 실질적이고 위로가 되는 조언을 해주세요. 존댓말을 사용하세요. |
| `coding` | 💻 | 코딩 | 당신은 프로그래밍 전문가입니다. 코드 관련 질문에 명확한 설명과 예시 코드를 제공하세요. 한국어로 설명하되, 코드와 기술 용어는 영어 원문을 유지하세요. 존댓말을 사용하세요. |
| `creative` | ✨ | 창작 | 당신은 창의적인 한국어 작가입니다. 시, 소설, 에세이, 카피라이팅 등 창작 활동을 도와주세요. 한국어의 아름다움을 살리면서 독창적인 표현을 사용하세요. 존댓말을 사용하세요. |

---

## Layout

### Category Selection Integration

The category selector and system prompt editor insert into the existing battle page between the header and the battle input. The primary visual anchor in idle state remains the BattleInput + "배틀 시작" CTA -- the CategorySelector sits above as a lightweight precondition, not a competing focal point. The overall page structure becomes:

```
+--------------------------------------------------+
|  Header: "K-Index" logo + subtitle               |  48px height
+--------------------------------------------------+
|                                                  |
|  [CategorySelector]                              |  NEW: Category
|  💬 일반  📚 과제  📝 자소서  🤗 상담  💻 코딩  ✨ 창작    |  toggle group
|                                                  |
+--------------------------------------------------+
|                                                  |
|  [SystemPromptEditor] (collapsible, collapsed)   |  NEW: Expandable
|  > 시스템 프롬프트 보기                              |  prompt editor
|                                                  |
+--------------------------------------------------+
|                                                  |
|  [BattleInput]                                   |  Existing (unchanged)
|  "한국어로 질문을 입력하세요"                        |  + CTA button
|                                                  |
+--------------------------------------------------+
|  ... (rest of battle flow unchanged) ...         |
+--------------------------------------------------+
```

### CategorySelector Layout

```
+--------------------------------------------------+
| 카테고리 선택                                      |  Label (14px, muted-foreground)
+--------------------------------------------------+
| [💬 일반] [📚 과제] [📝 자소서] [🤗 상담] [💻 코딩] [✨ 창작] |
+--------------------------------------------------+
```

- Horizontal row of toggle items on desktop (1024px+)
- Horizontal scrollable row on tablet (768px-1023px) with scroll indicators
- 2x3 grid on mobile (< 768px) for touch-friendly targets
- Each toggle item: emoji + Korean label, 44px minimum height
- Selected item: `--primary` background, `--primary-foreground` text, 2px primary border, 700 (bold) weight
- Unselected items: `--background` background, `--foreground` text, 1px `--border` border, 400 (regular) weight
- Hover (unselected): `--muted` background

### SystemPromptEditor Layout (expanded)

```
+--------------------------------------------------+
| > 시스템 프롬프트 보기                    [기본값 복원] |  Trigger row
+--------------------------------------------------+
| +----------------------------------------------+ |
| | 당신은 한국어 AI 어시스턴트입니다...               | |  Textarea (14px)
| |                                              | |  min-height: 80px
| |                                              | |  max-height: 200px
| +----------------------------------------------+ |
| 0 / 500                                         |  Char counter (mono)
+--------------------------------------------------+
```

- Collapsed by default (saves vertical space for the core battle flow)
- Trigger is a text button with chevron icon: "시스템 프롬프트 보기" / "시스템 프롬프트 숨기기"
- When expanded: textarea shows the current category's default system prompt
- "기본값 복원" (Restore default) button appears only when user has modified the prompt
- Maximum prompt length: 500 characters
- Textarea uses 14px font size (Label role) to visually subordinate it to the main question input

### Responsive Breakpoints

| Breakpoint | CategorySelector | SystemPromptEditor |
|------------|-----------------|-------------------|
| Mobile (< 768px) | 2x3 grid, each item full-width in column | Full width, collapsed default |
| Tablet (768px - 1023px) | Horizontal scroll row | Full width, collapsed default |
| Desktop (1024px+) | Single horizontal row, centered | Full width, max-width 1120px (matches battle container), collapsed default |

---

## Interaction States

### State Machine: Category + Battle Flow

```
[idle]
  |-- user selects category --> category updates, system prompt updates
  |     (if prompt was modified: inline warning before switching, see below)
  |-- user expands prompt editor --> shows current prompt
  |-- user edits prompt --> custom prompt stored per-category in local state
  |-- user collapses prompt editor --> edits preserved
  |-- user submits question --> [streaming] (with selected category + prompt)

[streaming / voting / reveal / error]
  |-- category selector: DISABLED (read-only, shows selected category)
  |-- prompt editor: HIDDEN (collapsed, trigger hidden)
  |-- all other states: same as Phase 02

[reveal]
  |-- user clicks "new battle" --> [idle] (category selection preserved, prompt reset to default)
```

### Per-State Visual Contract

#### 1. Idle State (category selection active)

- **Focal point:** BattleInput textarea and "배틀 시작" CTA remain the primary visual anchor, consistent with Phase 02. CategorySelector is a lightweight precondition above the input, not a competing focal point.
- **CategorySelector visible:** All toggle items enabled, one selected (default: "general")
- **SystemPromptEditor visible:** Collapsed by default, expandable
- **BattleInput visible:** Same as Phase 02 idle state
- When user selects a different category:
  - If `isPromptModified` is true for the current category, an inline warning appears directly below the CategorySelector: "수정한 프롬프트가 있습니다. 카테고리를 변경하시겠습니까?" with two inline text buttons: "변경" (confirm switch, muted style) and "취소" (cancel, returns to current category). The warning uses 14px label size, `--destructive` text color, and 200ms fade-in. If the user confirms, the switch proceeds and the custom prompt for the previous category is discarded. If cancelled, the category selection reverts and no state changes.
  - If `isPromptModified` is false, the switch proceeds immediately with no warning.
  - Toggle item transitions: 200ms ease-out background color change
  - System prompt textarea updates to new category's default prompt
  - Sonner toast NOT shown for category switch (too frequent, would be annoying)
- When user edits the system prompt:
  - "기본값 복원" button fades in (200ms)
  - A small "수정됨" (modified) badge appears next to the collapsible trigger
  - Editing does not affect the default -- only the current session's battle
- When user clicks "기본값 복원":
  - Textarea content resets to the selected category's default prompt
  - "기본값 복원" button fades out
  - "수정됨" badge disappears

#### 2. Streaming State (category locked)

- **CategorySelector:** Selected item shown with primary styling, all items `pointer-events-none`, `opacity-60` on unselected items
- **SystemPromptEditor:** Collapsed and trigger hidden (no need to show prompt during streaming)
- **Rest of battle flow:** Identical to Phase 02

#### 3. Voting State

- **CategorySelector:** Same as streaming (locked, selected visible)
- **SystemPromptEditor:** Hidden
- **Rest of battle flow:** Identical to Phase 02

#### 4. Reveal State

- **CategorySelector:** Same as streaming (locked, selected visible)
- **SystemPromptEditor:** Hidden
- Win rates shown are per-category (already supported by `getModelWinRates(category)` in vote route)
- "New battle" resets to idle with the same category pre-selected but prompt reset to default

#### 5. Error State

- **CategorySelector:** Same as streaming (locked)
- **SystemPromptEditor:** Hidden
- Error display and retry: identical to Phase 02
- Retry resets to idle with category preserved

---

## Copywriting Contract

All copy in Korean with formal polite speech. Inherits Phase 02 copy; additions for Phase 03 listed below.

| Element | Korean Copy | English Gloss |
|---------|-------------|---------------|
| Category section label | 카테고리 선택 | Select Category |
| Category: general | 일반 | General |
| Category: homework | 과제 도움 | Homework Help |
| Category: cover-letter | 자기소개서 | Cover Letter |
| Category: counseling | 고민 상담 | Counseling |
| Category: coding | 코딩 | Coding |
| Category: creative | 창작 | Creative |
| Prompt trigger (collapsed) | 시스템 프롬프트 보기 | View System Prompt |
| Prompt trigger (expanded) | 시스템 프롬프트 숨기기 | Hide System Prompt |
| Prompt modified badge | 수정됨 | Modified |
| Restore default button | 기본값 복원 | Restore Default |
| Prompt char counter | {n} / 500 | {n} / 500 |
| Prompt placeholder | 이 카테고리의 시스템 프롬프트를 수정할 수 있습니다 | You can edit this category's system prompt |
| Prompt too long validation | 시스템 프롬프트는 500자 이내로 입력해주세요 | Please keep the system prompt under 500 characters |
| Prompt empty validation | 시스템 프롬프트를 입력해주세요 | Please enter a system prompt |
| Category switch warning | 수정한 프롬프트가 있습니다. 카테고리를 변경하시겠습니까? | You have an edited prompt. Switch categories? |
| Category switch confirm | 변경 | Switch |
| Category switch cancel | 취소 | Cancel |
| Win rate category label | {카테고리명} 카테고리 승률 | {category} Category Win Rates |

### Inherited from Phase 02 (unchanged)

| Element | Korean Copy |
|---------|-------------|
| Page title | K-Index |
| Page subtitle | AI 블라인드 배틀 |
| Input placeholder | 한국어로 질문을 입력하세요 |
| Primary CTA | 배틀 시작 |
| Vote prompt | 어떤 응답이 더 좋았나요? |
| Vote A / Vote B | A 승리 / B 승리 |
| New battle CTA | 새 배틀 시작 |
| Empty state heading | 첫 번째 배틀을 시작해보세요! |
| Empty state body | 한국어로 질문을 입력하면 두 AI가 경쟁합니다. 더 좋은 답변을 선택해주세요. |
| Error state message | 응답을 생성하는 중 오류가 발생했습니다 |
| Error retry button | 다시 시도 |

---

## Animation & Transitions

| Element | Animation | Duration | Easing |
|---------|-----------|----------|--------|
| Category toggle select | Background color fill from transparent to primary | 200ms | ease-out |
| Category toggle deselect | Background color from primary to transparent | 200ms | ease-out |
| Collapsible expand | Height from 0 to auto + fade in content | 250ms | ease-out |
| Collapsible collapse | Height from auto to 0 + fade out content | 200ms | ease-in |
| Chevron icon rotation | Rotate 0deg to 180deg on expand, reverse on collapse | 200ms | ease-out |
| "Modified" badge appear | Fade in + scale 0.9 to 1.0 | 200ms | ease-out |
| "Restore default" button appear | Fade in | 200ms | ease-out |
| Category switch warning appear | Fade in | 200ms | ease-out |
| Category switch warning dismiss | Fade out | 150ms | ease-in |

Inherited from Phase 02 (all battle flow animations unchanged).

---

## Accessibility Contract

Inherited from Phase 02, with Phase 03 additions:

| Requirement | Implementation |
|-------------|---------------|
| Category selection keyboard | Arrow keys navigate toggle group items, Enter/Space to select |
| Toggle group role | `role="radiogroup"` with `role="radio"` on each item, `aria-checked` on selected |
| Collapsible keyboard | Enter/Space on trigger toggles expanded state |
| Collapsible ARIA | `aria-expanded` on trigger, `aria-controls` pointing to content panel |
| Prompt textarea label | `aria-label="시스템 프롬프트 편집"` on the system prompt textarea |
| Category switch warning | `role="alert"` on the inline warning so screen readers announce it immediately when it appears |
| Tab order | Category toggles -> prompt trigger -> (if expanded) prompt textarea -> restore default -> question textarea -> submit button |
| Focus indicators | 2px primary ring with 2px offset (inherited from Phase 02) |
| Color contrast | All toggle states meet WCAG 2.1 AA (4.5:1 for label text) |
| Motion reduction | Respect `prefers-reduced-motion`: instant toggle transitions, no collapsible animation |

---

## Zustand Store Additions

Phase 03 extends the existing battle store with category state:

```typescript
// New state fields
category: string;             // Selected category ID (default: "general")
systemPrompt: string;         // Current system prompt (default from category)
isPromptModified: boolean;    // Whether user edited the default prompt
pendingCategory: string | null; // Category awaiting confirmation when prompt is modified

// New actions
setCategory: (categoryId: string) => void;  // Sets category, resets prompt to default
confirmCategorySwitch: () => void;           // Confirms pending category switch, discards custom prompt
cancelCategorySwitch: () => void;            // Cancels pending category switch, reverts to current
setSystemPrompt: (prompt: string) => void;  // Sets custom prompt, marks as modified
resetPrompt: () => void;                     // Resets prompt to selected category's default
```

The `setCategory` action checks `isPromptModified` before switching. If true, it sets `pendingCategory` instead of switching immediately, triggering the inline warning UI. `confirmCategorySwitch` applies the pending category and resets the prompt. `cancelCategorySwitch` clears `pendingCategory` without changing state.

The `startBattle` action must now include `category` and `systemPrompt` in the battle start request. The `reset` action preserves the selected `category` but resets `systemPrompt` to the category default and clears `isPromptModified`.

---

## API Contract Changes

### POST `/api/battle/start`

Request body adds:
```typescript
{
  question: string;
  category: string;        // NEW: category ID
  systemPrompt: string;    // NEW: system prompt to use
}
```

The `category` and `systemPrompt` are included in the signed HMAC token so the stream route can use them without trusting client-side state.

### POST `/api/battle/stream`

No request body changes. The stream route reads `category` and `systemPrompt` from the verified HMAC token instead of using the hardcoded `BATTLE_CONFIG.systemPrompt`.

### POST `/api/battle/vote`

No request body changes. The vote route reads `category` from the verified HMAC token instead of hardcoding `"general"`.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | button, card, textarea, badge, separator, skeleton (Phase 02) | not required |
| shadcn official | toggle-group, collapsible (Phase 03) | not required |

No third-party registries are used in Phase 03.

---

## Implementation Notes

### Category Data Structure

Categories are defined as a static array in a shared module (e.g., `src/lib/categories.ts`). No database table needed for v1 -- categories are code-defined constants.

```typescript
interface CategoryDef {
  id: string;
  emoji: string;
  label: string;           // Korean display name
  defaultPrompt: string;   // Default system prompt
}
```

### Integration with Existing Battle Flow

1. `BattleArena` adds `CategorySelector` and `SystemPromptEditor` above `BattleInput`
2. `BattleArena.handleStartBattle` passes `category` and `systemPrompt` to `/api/battle/start`
3. `BattleSession` type (HMAC token payload) adds `cat: string` and `sp: string` fields
4. Stream route reads `session.sp` instead of `BATTLE_CONFIG.systemPrompt`
5. Vote route reads `session.cat` instead of hardcoded `"general"`
6. `RevealPanel` shows category name in win rate section header

### System Prompt Character Limit

500 characters maximum. This balances:
- Enough room for meaningful prompt customization
- Not so much that it significantly increases token costs (system prompt tokens are billed on every API call)
- Korean characters average ~2-3 tokens each, so 500 chars = ~1000-1500 tokens of system prompt

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
