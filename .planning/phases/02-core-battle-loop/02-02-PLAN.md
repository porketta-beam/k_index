---
phase: 02-core-battle-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/layout.tsx
  - src/app/globals.css
  - components.json
  - src/lib/utils.ts
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/skeleton.tsx
autonomous: true
requirements:
  - BATTLE-02
  - BATTLE-03

must_haves:
  truths:
    - "shadcn/ui is initialized and all 6 required components are available for import"
    - "Zustand and sonner are installed and importable"
    - "Pretendard Variable font loads for Korean text rendering"
    - "CSS variables for all UI-SPEC colors are defined in globals.css"
    - "Korean word-break rule is applied globally"
  artifacts:
    - path: "components.json"
      provides: "shadcn/ui configuration for Tailwind v4 + src directory"
    - path: "src/lib/utils.ts"
      provides: "cn() helper for shadcn components"
      exports: ["cn"]
    - path: "src/components/ui/button.tsx"
      provides: "shadcn Button component"
    - path: "src/components/ui/card.tsx"
      provides: "shadcn Card component"
    - path: "src/components/ui/textarea.tsx"
      provides: "shadcn Textarea component"
    - path: "src/app/globals.css"
      provides: "CSS variables for all UI-SPEC colors including custom --success, --reveal-a, --reveal-b"
      contains: "Pretendard Variable"
  key_links:
    - from: "src/app/layout.tsx"
      to: "CDN"
      via: "Pretendard Variable font stylesheet link"
      pattern: "pretendardvariable"
    - from: "src/app/globals.css"
      to: "Tailwind"
      via: "@theme inline with --font-sans using Pretendard"
      pattern: "Pretendard Variable"
---

<objective>
Initialize shadcn/ui, install client-side dependencies (Zustand, sonner), set up Pretendard Korean font, and configure all CSS variables from the UI-SPEC design system.

Purpose: All battle UI components in Plans 03 and 04 depend on shadcn/ui components, Zustand for state management, and the design system CSS variables. This plan establishes that foundation.

Output: Working shadcn/ui with 6 components, Zustand + sonner installed, Pretendard font loading, complete CSS variable set matching UI-SPEC.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-battle-loop/02-CONTEXT.md
@.planning/phases/02-core-battle-loop/02-UI-SPEC.md

<interfaces>
<!-- Existing code contracts -->

From src/app/layout.tsx:
```typescript
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
// Geist fonts loaded with CSS variables --font-geist-sans and --font-geist-mono
// html lang="ko" already set
```

From src/app/globals.css:
```css
@import "tailwindcss";
:root { --background: #ffffff; --foreground: #171717; }
@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, initialize shadcn/ui, add required components</name>
  <files>package.json, components.json, src/lib/utils.ts, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/textarea.tsx, src/components/ui/badge.tsx, src/components/ui/separator.tsx, src/components/ui/skeleton.tsx</files>

  <read_first>
    package.json
    src/app/globals.css
    .planning/phases/02-core-battle-loop/02-UI-SPEC.md
    .planning/phases/02-core-battle-loop/02-RESEARCH.md
  </read_first>

  <action>
**Step 1: Install Zustand and sonner:**

```bash
npm install zustand sonner
```

Expected: zustand@5.x and sonner@2.x added to dependencies in package.json.

**Step 2: Initialize shadcn/ui:**

Run the shadcn init command. Since this is non-interactive in CI/executor context, use flags to pre-configure:

```bash
npx shadcn@latest init -d
```

The `-d` flag uses defaults. If this does not work non-interactively, manually create the required files:

**Manual `components.json`** (only if `npx shadcn init -d` fails):
```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "default",
  "rsc": true,
  "tsx": true,
  "tailwind": {
    "config": "",
    "css": "src/app/globals.css",
    "baseColor": "neutral",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils",
    "ui": "@/components/ui",
    "lib": "@/lib",
    "hooks": "@/hooks"
  },
  "iconLibrary": "lucide"
}
```

**Manual `src/lib/utils.ts`** (only if shadcn init doesn't create it):
```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

If creating utils.ts manually, also install its dependencies:
```bash
npm install clsx tailwind-merge
```

**Step 3: Add shadcn components:**

```bash
npx shadcn@latest add button card textarea badge separator skeleton
```

This will create files in `src/components/ui/`. If the add command requires confirmation, use the `--yes` or `--overwrite` flag:
```bash
npx shadcn@latest add button card textarea badge separator skeleton --overwrite
```

**Step 4: Verify all 6 component files exist:**
- `src/components/ui/button.tsx`
- `src/components/ui/card.tsx`
- `src/components/ui/textarea.tsx`
- `src/components/ui/badge.tsx`
- `src/components/ui/separator.tsx`
- `src/components/ui/skeleton.tsx`

**Step 5: Verify TypeScript compiles:**
```bash
npx tsc --noEmit
```

If tsc fails due to shadcn-generated code, fix the issues (common: missing dependencies, import path issues).
  </action>

  <verify>
    <automated>npx tsc --noEmit && test -f src/components/ui/button.tsx && test -f src/components/ui/card.tsx && test -f components.json && echo "PASS"</automated>
  </verify>

  <acceptance_criteria>
    - package.json contains "zustand" in dependencies
    - package.json contains "sonner" in dependencies
    - components.json exists at project root
    - components.json contains `"style": "default"` or `"style": "new-york"`
    - src/lib/utils.ts exists and contains `export function cn`
    - src/components/ui/button.tsx exists
    - src/components/ui/card.tsx exists
    - src/components/ui/textarea.tsx exists
    - src/components/ui/badge.tsx exists
    - src/components/ui/separator.tsx exists
    - src/components/ui/skeleton.tsx exists
    - npx tsc --noEmit exits 0
  </acceptance_criteria>

  <done>Zustand 5.x, sonner, and shadcn/ui are installed. All 6 required shadcn components (button, card, textarea, badge, separator, skeleton) are available for import. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Pretendard font, CSS variables, and Korean typography</name>
  <files>src/app/layout.tsx, src/app/globals.css</files>

  <read_first>
    src/app/layout.tsx
    src/app/globals.css
    .planning/phases/02-core-battle-loop/02-UI-SPEC.md
  </read_first>

  <action>
**1. Update `src/app/layout.tsx`** -- Add Pretendard Variable font CDN link and Toaster:

Add a Pretendard `<link>` stylesheet to the `<head>` section. In Next.js 16 App Router, add it inside the `<html>` before `<body>`, or use the `<head>` element.

The updated layout should include:

```tsx
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Toaster } from "sonner";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "K-Index | AI 블라인드 배틀",
  description:
    "한국어 AI 블라인드 배틀 - 어떤 AI가 더 자연스러운 한국어 답변을 할까?",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="ko"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <head>
        <link
          rel="stylesheet"
          href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable-dynamic-subset.min.css"
        />
      </head>
      <body className="min-h-full flex flex-col">
        {children}
        <Toaster position="bottom-center" richColors />
      </body>
    </html>
  );
}
```

Key changes:
- Added Pretendard Variable CDN `<link>` in `<head>`
- Added `<Toaster>` from sonner for toast notifications (vote recorded, errors)
- Updated metadata title to Korean per UI-SPEC copywriting contract
- Import `Toaster` from `"sonner"`

**2. Update `src/app/globals.css`** -- Replace entire file with shadcn + UI-SPEC design system variables:

After shadcn init, globals.css will have been modified by shadcn. Now update it to include ALL UI-SPEC color variables and Pretendard font stack. The final globals.css must contain:

```css
@import "tailwindcss";

@layer base {
  :root {
    --background: 0 0% 100%;
    --foreground: 240 10% 3.9%;
    --card: 0 0% 100%;
    --card-foreground: 240 10% 3.9%;
    --popover: 0 0% 100%;
    --popover-foreground: 240 10% 3.9%;
    --primary: 217.2 91.2% 59.8%;
    --primary-foreground: 0 0% 100%;
    --secondary: 240 4.8% 95.9%;
    --secondary-foreground: 240 5.9% 10%;
    --muted: 240 4.8% 95.9%;
    --muted-foreground: 240 3.8% 46.1%;
    --accent: 240 4.8% 95.9%;
    --accent-foreground: 240 5.9% 10%;
    --destructive: 0 84.2% 60.2%;
    --destructive-foreground: 0 0% 100%;
    --border: 240 5.9% 90%;
    --input: 240 5.9% 90%;
    --ring: 217.2 91.2% 59.8%;
    --radius: 0.5rem;

    /* Custom UI-SPEC colors */
    --success: 142.1 76.2% 36.3%;
    --success-foreground: 0 0% 100%;
    --reveal-a: 263.4 70% 50.4%;
    --reveal-a-foreground: 0 0% 100%;
    --reveal-b: 188.7 94.5% 42.7%;
    --reveal-b-foreground: 0 0% 100%;
  }
}

@theme inline {
  --color-background: hsl(var(--background));
  --color-foreground: hsl(var(--foreground));
  --color-card: hsl(var(--card));
  --color-card-foreground: hsl(var(--card-foreground));
  --color-popover: hsl(var(--popover));
  --color-popover-foreground: hsl(var(--popover-foreground));
  --color-primary: hsl(var(--primary));
  --color-primary-foreground: hsl(var(--primary-foreground));
  --color-secondary: hsl(var(--secondary));
  --color-secondary-foreground: hsl(var(--secondary-foreground));
  --color-muted: hsl(var(--muted));
  --color-muted-foreground: hsl(var(--muted-foreground));
  --color-accent: hsl(var(--accent));
  --color-accent-foreground: hsl(var(--accent-foreground));
  --color-destructive: hsl(var(--destructive));
  --color-destructive-foreground: hsl(var(--destructive-foreground));
  --color-border: hsl(var(--border));
  --color-input: hsl(var(--input));
  --color-ring: hsl(var(--ring));

  /* Custom UI-SPEC theme colors */
  --color-success: hsl(var(--success));
  --color-success-foreground: hsl(var(--success-foreground));
  --color-reveal-a: hsl(var(--reveal-a));
  --color-reveal-a-foreground: hsl(var(--reveal-a-foreground));
  --color-reveal-b: hsl(var(--reveal-b));
  --color-reveal-b-foreground: hsl(var(--reveal-b-foreground));

  --font-sans: 'Pretendard Variable', var(--font-geist-sans), system-ui, sans-serif;
  --font-mono: var(--font-geist-mono);
  --radius-sm: calc(var(--radius) - 4px);
  --radius-md: var(--radius);
  --radius-lg: calc(var(--radius) + 4px);
}

@layer base {
  body {
    background-color: var(--color-background);
    color: var(--color-foreground);
  }

  /* Korean typography: prevent mid-syllable line breaks (UI-SPEC) */
  body {
    word-break: keep-all;
  }
}
```

**IMPORTANT NOTES:**
- The `--primary` HSL value `217.2 91.2% 59.8%` corresponds to blue-600 (#2563eb) from UI-SPEC
- The `--destructive` HSL value `0 84.2% 60.2%` corresponds to red-600 (#dc2626) from UI-SPEC
- The `--success` HSL value `142.1 76.2% 36.3%` corresponds to green-600 (#16a34a) from UI-SPEC
- The `--reveal-a` HSL value `263.4 70% 50.4%` corresponds to violet-600 (#7c3aed) from UI-SPEC
- The `--reveal-b` HSL value `188.7 94.5% 42.7%` corresponds to cyan-600 (#0891b2) from UI-SPEC
- The `--font-sans` uses Pretendard Variable as primary, Geist Sans as fallback (per UI-SPEC font setup)
- `word-break: keep-all` prevents Korean syllable splitting (UI-SPEC typography note)
- shadcn init may modify this file -- after init, ensure ALL the above variables are present. If shadcn overwrites custom variables, re-add them.
- If shadcn uses a different CSS variable format (e.g., oklch instead of HSL), adapt the values to match the format while maintaining the same visual colors.
  </action>

  <verify>
    <automated>npx tsc --noEmit && grep -c "Pretendard" src/app/globals.css && grep -c "reveal-a" src/app/globals.css && grep -c "pretendard" src/app/layout.tsx</automated>
  </verify>

  <acceptance_criteria>
    - src/app/layout.tsx contains `pretendardvariable-dynamic-subset.min.css`
    - src/app/layout.tsx contains `import { Toaster } from "sonner"`
    - src/app/layout.tsx contains `<Toaster`
    - src/app/layout.tsx contains `AI 블라인드 배틀` in metadata title
    - src/app/globals.css contains `Pretendard Variable`
    - src/app/globals.css contains `--success`
    - src/app/globals.css contains `--reveal-a`
    - src/app/globals.css contains `--reveal-b`
    - src/app/globals.css contains `word-break: keep-all`
    - src/app/globals.css contains `--font-sans`
    - npx tsc --noEmit exits 0
  </acceptance_criteria>

  <done>Pretendard Korean font loads via CDN, sonner Toaster is mounted in root layout, all UI-SPEC CSS variables (including custom success, reveal-a, reveal-b) are defined, Korean word-break rule is applied globally. TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CDN -> Client | Pretendard font loaded from jsdelivr CDN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Tampering | CDN font | accept | jsDelivr is a trusted, widely-used CDN. Font is a CSS stylesheet, low risk. Could add SRI hash for stronger integrity if needed in future. |
| T-02-10 | Information Disclosure | sonner Toaster | accept | Toast messages contain only user-facing copy (no secrets). Position bottom-center, auto-dismiss. |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes
2. `npm run build` passes (no build errors)
3. All 6 shadcn UI components exist in src/components/ui/
4. Pretendard font CDN link present in layout.tsx
5. CSS variables for success, reveal-a, reveal-b defined in globals.css
6. Korean word-break: keep-all applied in globals.css
</verification>

<success_criteria>
- shadcn/ui initialized with components.json and 6 components (button, card, textarea, badge, separator, skeleton)
- Zustand 5.x and sonner installed in package.json
- Pretendard Variable font CDN link in layout.tsx head
- Sonner Toaster component mounted in root layout
- globals.css contains all shadcn CSS variables plus custom UI-SPEC colors (success, reveal-a, reveal-b)
- Font stack: Pretendard Variable > Geist Sans > system-ui > sans-serif
- word-break: keep-all applied globally for Korean text
- TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-battle-loop/02-02-SUMMARY.md`
</output>
